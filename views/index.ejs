
    <%- include('elems/navbar') %>
    <div class="container" style="max-width: 600px;">
        <p>Blog post CMS based on just 1 empty array which is used as a database. You can add, delete and edit blog posts. Try it out)</p>
        <a href="/add"><button class="btn btn-primary mb-3 mt-3">Add new</button></a>

        <% for (post of blogposts){ %>
        <div class="card mb-2">
            <div class="card-body">
                <h5 class="card-title"><%= post.title %></h5>
                <h6 class="card-subtitle mb-2 text-muted"><%= post.date %></h6>
                <p class="card-text"><%= post.text %></p>
                <a href="/delete/<%= post.id %>" class="card-link">Delete</a>
                <a href="/edit/<%= post.id %>" class="card-link">Edit</a>
            </div>
        </div>
        <% } %>
    
    </div>

</body>

</html>